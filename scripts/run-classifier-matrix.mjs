/**
 * Placeholder entry for the labeled chaos matrix.
 * Full scenarios live in src/netaccess/scenarios.ts in the interactive console build.
 * Target: >=95% top-1 classification accuracy on the fixture suite.
 */
console.log("NetAccess classifier matrix");
console.log("Run the Chaos Lab in the interactive console for the full 15-scenario KPI.");
console.log("Expected fixtures: healthy, captive, nxdomain, servfail, dns-lie, ipv6-blackhole,");
console.log("  refused, tcp-timeout, peering, pmtu, tls-hang, bad-cert, sni-block, http-403, http-451");
process.exit(0);
